---
driver:
  name: docker

driver_config:
  use_sudo: false

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-12.04
  - name: ubuntu-14.04
  - name: centos-6.5
  - name: centos-7.0
  - name: fedora-21

suites:
  - name: default
    run_list:
      - recipe[hekad]
      - recipe[setup]
    attributes:
